// Create a function that accepts a parameter representing a name and returns the 
// message: "Hello, <name> how are you doing today?".


export const greet = (name: string): string => `Hello, ${name} how are you doing today?`